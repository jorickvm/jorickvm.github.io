Landing page for [AtlasDays](https://atlasdays.app) — a private iPhone app for tracking trips, visa days, and residency thresholds.

## Website launch TODO

- Replace the current temporary screenshots in `index.html` (`assets/screenshot1.png` and `assets/screenshot2.png`) once the next product captures are ready.
- Capture a dashboard overview with tracker cards, summary stats, and the map together.
- Capture the tracker preset picker and one tracker detail view.
- Capture the trip form in both `Exact Dates` and `Month & Year` modes, including an example with `Dates Unknown`.
- Capture the CSV import or import-preview flow, ideally including the AI prompt helper.
- Capture one privacy/location screen if that flow is polished enough to show publicly.
